syntax = "proto3";

service ChatService {
  rpc ProcessMessage (ChatRequest) returns (ChatResponse);
}

message ChatRequest {
  string user_id = 1;
  string message = 2;
}

message ChatResponse {
  string response = 1;
}

